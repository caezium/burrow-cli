# Optional release-build analytics. Source builds without a key keep telemetry off.
# Use your own project's write-only capture token. This is a placeholder, not a credential.
# Export these variables before building; CI may supply them as repository secrets.
export BURROW_POSTHOG_KEY="phc_REPLACE_ME"
export BURROW_POSTHOG_HOST="https://us.i.posthog.com"
